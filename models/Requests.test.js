import Requests from './Requests';

describe('Requests.extractClipNames', () => {
  it('Should be an instance of Function', () => {
    expect(Requests.extractClipNames).toBeInstanceOf(Function);
  });
});
